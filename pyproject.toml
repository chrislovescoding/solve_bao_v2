[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bao-kujifunza-solver"
version = "0.1.0"
description = "Reference engine scaffolding for Bao la Kujifunza"
requires-python = ">=3.12"

